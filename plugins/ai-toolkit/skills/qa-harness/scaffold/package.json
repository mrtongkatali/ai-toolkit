{
  "name": "qa-harness",
  "private": true,
  "version": "0.1.0",
  "description": "Throwaway Playwright runtime for the ai-toolkit qa-harness skill.",
  "type": "module",
  "engines": {
    "node": ">=24.11.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:dev": "playwright test --project=dev",
    "test:staging": "playwright test --project=staging"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "dotenv": "^16.4.5"
  }
}
